name: Test Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Run tests
        run: |
          echo "Running tests..."
          echo "Test 1: PASS"
          echo "Test 2: PASS"
          echo "Test 3: FAIL"
          exit 1  # This will make the workflow fail and trigger CI Notifier
