# Environment Configuration

# Server
PORT=3000
NODE_ENV=development

# Database (leave DATABASE_URL empty to use SQLite)
# For SQLite: data/ci_runs.db (auto-created)
# For Postgres: postgresql://user:password@host:5432/dbname
DATABASE_URL=

# Zoho Cliq Incoming Webhook URL
# Get this from Cliq: Settings > Bots & Tools > Incoming Webhooks
CLIQ_WEBHOOK_URL=

# Backend URL (for Cliq extension and ngrok)
# Local: http://localhost:3000
# ngrok: https://your-ngrok-url.ngrok.io
# Production: https://your-app.herokuapp.com
BACKEND_URL=http://localhost:3000

# GitHub Personal Access Token (optional, for rerun & create issue features)
# Generate at: https://github.com/settings/tokens
# Required scopes: repo, workflow
GITHUB_TOKEN=

# API Key for dashboard endpoints (optional, recommended for production)
# Generate a random string, e.g.: openssl rand -hex 32
API_KEY=

# Webhook signature secret (optional, for webhook verification)
# Used for verifying incoming webhooks from GitHub, etc.
WEBHOOK_SECRET=
